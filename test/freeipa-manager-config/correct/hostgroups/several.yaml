---
group-two:
  description: Sample meta host group two.
  memberOf:
    hostgroups:
      - group-one-hosts

group-three-hosts:
  description: Sample host group three.
  memberOf:
    hostgroups:
      - group-two
